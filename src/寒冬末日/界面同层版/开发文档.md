# 界面同层版开发文档

## 1. 项目目标

`界面同层版` 位于 `src/寒冬末日/界面同层版/`，它以 `src/流式最小Demo/` 为业务基底，迁入 `docs/UI/`
的 UI/UX 组织方式，形成更适合移动端和同层 iframe 的流式阅读工作台。

本次迁移坚持两个原则：

1. **业务逻辑不改向**：`generate`、楼层写回、transcript 重建、开局 payload、回滚/重生成功能全部沿用原 Demo。
2. **壳层体验重构**：把 `docs/UI` 的顶栏、抽屉、弹窗、状态概览和 HUD 语气迁到 Vue 结构中。

---

## 2. 目录结构

```text
src/寒冬末日/界面同层版/
├── shared/                     # 直接继承流式最小Demo的业务共享层
├── 界面/
│   └── 状态栏/
│       ├── App.vue            # 新的页面壳层与英雄区
│       ├── useStreamingDemo.ts # 核心业务逻辑（保留）
│       ├── pages/StoryPage.vue # 工作台布局、抽屉和弹窗编排
│       └── components/
│           └── HudModal.vue   # 新增通用 HUD 弹窗容器
└── 开发文档.md
```

---

## 3. 功能 → UI 映射

### 3.1 从 `流式最小Demo` 保留的业务能力

- 开局世界模式 / 路线 / 首段生成
- transcript 读取、重建与过滤
- 同层流式发送、楼层隐藏与最终写回
- 内联编辑用户输入并重新生成
- 删除回滚、助手 swipe 切换
- 阅读模式状态持久化（theme / density / opening fold）
- 工作台日志记录

### 3.2 从 `docs/UI` 迁入的 UI 结构

| `docs/UI` 模板能力             | `界面同层版` 映射位置                                      | 说明                                     |
| ------------------------------ | ---------------------------------------------------------- | ---------------------------------------- |
| `TopBar`                       | `StoryPage.vue` 顶部 `command-bar` + `TopToolbar` 设置弹窗 | 主舞台上保留状态摘要，详细控制收纳进弹窗 |
| `CharacterSidebar`             | `role-drawer` + `MvuRolePanel`                             | 右侧角色抽屉，移动端改为底部上滑         |
| `TasksModal`                   | `HudModal + WorkbenchTabs`                                 | 用于承接系统日志与链路诊断               |
| `MapModal`                     | `HudModal + OpeningSetupPanel`                             | 用于回访开局配置和重新生成               |
| `TypographySettingsModal`      | `HudModal + TopToolbar`                                    | 承接主题 / 密度 / 过滤 / 跳转等阅读控制  |
| `TranscriptList / MessageItem` | 原 Vue transcript 组件重用                                 | 保留业务渲染和交互，只改外层排版语气     |
| `BottomInput`                  | 原 `BottomComposer` 外包一层 `composer-dock`               | 强化底部操作停靠感                       |

---

## 4. 迁移策略说明

### 4.1 为什么不直接复制 `docs/UI` 代码

`docs/UI` 是 React + Tailwind + Vite 原型，而仓库目标项目是 Vue + Webpack +
iframe 运行形态。直接搬运会引入额外框架切换成本，并破坏现有业务代码稳定性。

因此本次采用：

- **业务层**：直接复制并沿用 `流式最小Demo`
- **视觉与交互层**：按 `docs/UI` 的模块拆法，用 Vue 重建等价壳层
- **样式层**：复用原 Demo 已整理好的 token / HUD 主题体系，再向 `docs/UI` 的层级组织靠拢

### 4.2 为什么把控制项塞进弹窗

同层 iframe 需要优先保证 transcript 阅读面积；移动端又要避免一屏内出现过多切换控件。因此：

- 主界面只保留摘要和高频动作
- 中低频设置收进弹窗
- 角色信息走侧抽屉

这比把所有开关都铺在顶部更适合长文本阅读。

---

## 5. 关键改动清单

### `src/寒冬末日/界面同层版/界面/状态栏/App.vue`

- 新增英雄区与状态标签
- 强化项目身份为“同层工作台”而不是临时 demo
- 引入更明显的 HUD 光晕与网格背景

### `src/寒冬末日/界面同层版/界面/状态栏/pages/StoryPage.vue`

- 增加 `command-bar` 总控条
- 增加左侧 `overview-rail` 状态概览
- 将 `WorkbenchTabs` 改为日志弹窗
- 将 `TopToolbar` 收纳为阅读设置弹窗
- 将 `OpeningSetupPanel` 收纳为开局设定弹窗
- 将 `MvuRolePanel` 改造成右侧角色抽屉
- 保留 `TranscriptList` 与 `BottomComposer` 的行为逻辑

### `src/寒冬末日/界面同层版/界面/状态栏/components/HudModal.vue`

- 新增通用弹窗壳层
- 支持 ESC 关闭、遮罩关闭、宽版模式
- 供日志 / 设置 / 开局三个弹窗复用

---

## 6. 验证建议

### 构建验证

在仓库根目录执行：

```bash
pnpm build
```

### 人工验证要点

1. 初次进入时，开局配置面板正常出现。
2. 生成开局后，界面切换到 transcript 主舞台。
3. 顶部按钮可以分别打开：系统日志 / 开局设定 / 阅读设置 / 人物面板。
4. transcript 内原有功能仍可使用：
   - 查看详情
   - 内联编辑用户输入
   - 删除回滚
   - assistant swipe
   - 回到最新
5. 切换主题和阅读密度后，聊天变量中的 reader state 仍会持久化。
6. 移动端宽度下，角色抽屉改为底部上滑，不出现横向滚动。

---

## 7. 后续可选优化

- 为 `command-bar` 增加更细分的模型 / token / 世界模式摘要
- 给 transcript 头部加虚拟滚动或折叠搜索
- 把 `docs/UI` 中更细的 typography 控制拆成独立设置项
- 为 `HudModal` 增加进入 / 离开动画层级差和焦点锁定
