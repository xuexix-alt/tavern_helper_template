# 同层卡参考要点

本文用于沉淀「同层卡」在酒馆助手（Tavern Helper）里的可复用实现方式，重点参考了 `幻想乡缘起ERA` 的实际运行形态。

## 1. 什么是同层卡

- 同层卡的核心是「消息楼层隔离」。
- 同一聊天中可能存在多个消息 iframe，每个 iframe 只应该渲染和操作自己的楼层数据。
- UI 与后台通过事件通信，不直接耦合宿主页面 DOM。

## 2. 推荐整体架构

- 消息层前台 UI（每条消息一个 iframe）：
  - 例如 `Starter`（开局页）、`FACE`（状态页）。
- 后台脚本 iframe（全局处理）：
  - 负责监听变量更新、做计算、发 UI 刷新事件。
- MVU / ERA 变量层：
  - 作为最终状态源，UI 只消费，不直接发散写入。

## 3. 正则加载建议（不依赖 `$1` 注入）

同层卡建议使用远程页面加载，不把正文注入到 HTML：

```html
<body>
  <script>
    $('body').load('https://onmm.top/2.0.4/FACE/index.html')
  </script>
</body>
```

要点：

- 正则只负责替换显示入口，不承担数据注入职责。
- 数据由前台在运行时通过 `getCurrentMessageId()` + `getChatMessages()` 读取。

## 4. 事件流（核心）

推荐最小闭环：

1. 后台在数据结算后发事件
   - `eventEmit('GSKO:showUI', payload)`
2. 前台收到后先做同层校验
   - `payload.message_id === getCurrentMessageId()`
3. 前台若是晚挂载，主动请求重放
   - `eventEmit('GSKO:requireData')`
4. 后台缓存最近一次 `GSKO:showUI` 参数，收到 `GSKO:requireData` 时重放

## 5. 同层隔离的硬性规则

- 必须携带并校验 `message_id`。
- 事件到达前台后，不匹配当前楼层则直接 `return`。
- 不要用全局「最新楼层」直接渲染当前 iframe。
- 如需锁层（如开局页），首次确定 `message_id` 后可只接受同 ID 刷新。

## 6. 正文提取策略（可配置标签）

推荐支持两组标签：

- `mainBodyTags`：主正文标签（如 `content`）
- `excludeBodyTags`：排除标签（如 `UpdateVariable`、`profile` 等）

这样可以兼容不同卡模板的自定义包裹标签，而不是写死单一标签名。

## 7. 发送入口建议

### 7.1 前台直接走 slash（常用）

```ts
const payload = { type: 'GENERATE_STORY_SCENE', guidelines };
const cmd = `/send ${JSON.stringify(payload)}|/trigger`;
await triggerSlash(cmd);
```

### 7.2 需要静默推理再落库（高级）

- 用 `generate({ should_stream: true, should_silence: true, generation_id })`。
- 结束后 `createChatMessages(..., { refresh: 'affected' })`，保证 MVU/同层 UI 一起刷新。

## 8. 建议优先使用的酒馆助手函数

- 楼层识别：
  - `getCurrentMessageId()`
- 消息读取与更新：
  - `getChatMessages()`
  - `setChatMessages()`
  - `createChatMessages()`
- 变量读写：
  - `getVariables()`
  - `updateVariablesWith()`
  - `replaceVariables()`
- 事件：
  - `eventOn()`
  - `eventEmit()`
  - `eventOnce()`
  - `eventRemoveListener()`
- 发送与重载：
  - `triggerSlash()`
  - `reloadIframe()`

## 9. 常见坑位

- 只靠 `getChatMessages(-1)` 拿最新楼层，导致多 iframe 串层。
- 前台先挂载但未收到历史事件，界面空白（未做 `requireData` 重放）。
- 正文强依赖固定标签名，不兼容自定义标签。
- 把正则当数据层使用，后续维护困难。
- 用全局选择器直接改宿主 DOM，破坏隔离边界。

## 10. 最小可复用模板

```ts
eventOn('GSKO:showUI', (detail) => {
  const mid = getCurrentMessageId();
  if (typeof detail?.message_id !== 'number') return;
  if (detail.message_id !== mid) return;
  // 同层才渲染
  render(detail);
});

eventEmit('GSKO:requireData');
```

## 11. 与生图插件协作建议（简版）

- 同层 UI 里统一事件桥接，不耦合插件 DOM：
  - 请求：`generate-image-request`
  - 响应：`generate-image-response`
- 用唯一 `requestId` 做回包匹配。
- 有缓存优先展示缓存图，无缓存再触发生图。
- 图片渲染后保持同层筛选语义（可被“图片”标签识别）。

## 12. 自检清单

- 是否做了 `message_id` 同层校验。
- 是否支持 `requireData` 重放。
- 是否支持可配置正文标签提取。
- 是否避免 `$1` 注入依赖。
- 是否只使用 `@types` 中允许的接口。

