# 寒冬末日｜界面同层版 · 流式传输性能优化施工说明

> 文档目的：把本次性能评估的结论、原理、具体改动点与验证方式整理成可直接交给施工 AI 的说明书。
> 适用范围：`src/寒冬末日/界面同层版/界面/状态栏/` 下的 Vue/TS 代码。
> 症状：手机端在 LLM 流式输出期间整页卡顿、滚动/输入有掉帧，桌面端相对顺滑。
> 基调：**不改变业务语义**，只压缩每次流式 tick 的工作量与合成成本。

---

## 0. 结论 TL;DR

流式卡顿的根因是 **每 80 ms 一次的 `patchAssistantMessage('stream')` 周期**同时触发了以下连锁开销，手机中低端 CPU/GPU 撑不住：

1. 为当前楼层跑 **两次** `buildFinalHtml`（重的一条：`formatAsDisplayedMessage` + `createHTMLDocument` 全量 parse + 跨 iframe 宿主 DOM selector + `appendChatu8ArtifactsToHtml`）。
2. `syncTranscriptFlags(clipTranscriptItemsForUi([...]))` 每次都产出全新的条目对象数组，破坏 Vue diff 的引用相等，导致所有可见卡片都 patch。
3. `TranscriptList.itemsSignature` 的签名里包含 `len: item.content.length`，token 每来一次，签名就变，watcher 跑一次完整滚动锚定逻辑。
4. `TranscriptMessageCard` 的 watch 签名里包含完整 HTML 字符串，流式阶段每 tick 都在做 KB 级 join+比较；随后 `v-html` 整块子树替换 + `hydrateAssistantBodyImages` + `bindAssistantBodyInteractions`。
5. 顶栏、侧栏、底栏、FAB、图片按钮等 9 处 `backdrop-filter: blur(10~18px)`，流式滚动期间合成层反复重算。
6. `recordLifecycleTrace` / `debugConsoleLog` 的 payload 对象（含 `summarizeTranscriptForDebug(transcript.value)`）即使开关关闭也会在调用前构造。

**预期收益**：把 P0 全部做掉，手机端单次 patch 的 JS 耗时可下降 60% 以上；合成层降到可接受数量后，滚动能恢复流畅。

---

## 1. 分析路径（可复现）

1. 读 `开发文档.md` 明确业务边界（消息 iframe 内运行，`useStreamingDemo.ts` 是业务中枢）。
2. 根据大小定位热点文件：
   - `useStreamingDemo.ts` ≈ 156 KB（业务中枢）
   - `StoryPage.vue` ≈ 97 KB（页面编排 + 宿主桥接）
   - `TranscriptMessageCard.vue` ≈ 24 KB（流式渲染终点）
   - `TranscriptList.vue` ≈ 19 KB（滚动/分页）
3. 跟链：`iframe_events.STREAM_TOKEN_RECEIVED_INCREMENTALLY` → `scheduleStreamTranscriptPatch` → `flushScheduledStreamTranscriptPatch` → `patchAssistantMessage('stream')` → `upsertTranscriptItem(createLocalTranscriptItem(...))` → `buildTranscriptItem` → `buildFinalHtml` × 2 / `applyRegexForDisplay` / `appendChatu8ArtifactsToHtml`。
4. 组件层跟链：`transcript.value` 重赋值 → `visibleTranscript` computed → `TranscriptList` 的 `itemsSignature` watcher → 卡片 props 变化 → `TranscriptMessageCard` 的 `assistantBodySignature` watcher → `v-html` + hydrate + rebind。
5. 合成层检查：`grep backdrop-filter` 命中 9 处。
6. 观察调试通道：`recordLifecycleTrace` / `summarizeTranscriptForDebug` 在流式路径上每 tick 都会调用。

---

## 2. 标注：关键代码位置

### 2.1 `src/寒冬末日/界面同层版/界面/状态栏/useStreamingDemo.ts`

```ts
// 约 171 行：流式节流间隔
const STREAM_TRANSCRIPT_PATCH_INTERVAL_MS = 80;
```

```ts
// 约 1262 行：buildTranscriptItem — 这里是每个 token 的 CPU 大户
// ⚠️ 流式阶段不应该同时算 streamHtml 和 finalHtml 两份
const streamHtml = isDemoAssistant
  ? buildFinalHtml(renderSource, input.id, strippedRenderSource)
  : buildFinalHtml(displayRenderSource, input.id, strippedRenderSource);
const finalHtml = buildFinalHtml(displayRenderSource, input.id, strippedRenderSource);
```

```ts
// 约 256 行：buildFinalHtml — 内部栈：formatAsDisplayedMessage + normalize + applyTranscriptArtifacts
// applyTranscriptArtifacts 会调 appendChatu8ArtifactsToHtml → 跨 iframe 查宿主 + 全量 HTML parse
function buildFinalHtml(renderSource: string, message_id: number, artifactSource?: string): string { ... }
```

```ts
// 约 611 行：appendChatu8ArtifactsToHtml — 流式期间没必要跑
function appendChatu8ArtifactsToHtml(html, renderSource, messageId) {
  const iframeRoots = resolveIframeAssistantRoots(messageId); // 跨 iframe 5 个 selector
  const nativeFirstImages = readNativeFirstRenderableImagesForMessage({ ... });
  // ...
  const existingSources = collectImageSourceIdentitiesFromHtml(html); // 每次都 createHTMLDocument parse
}
```

```ts
// 约 2189 行：syncTranscriptFlags — 无条件创建全部浅拷贝
function syncTranscriptFlags(items: TranscriptItem[]): TranscriptItem[] {
  return items.map(item => ({
    ...item,
    isLatest: assistantMessageId.value === item.message_id,
    isStreaming: assistantMessageId.value === item.message_id && (status.value === 'streaming' || item.phase === 'stream'),
  }));
}
```

```ts
// 约 3057 行：节流调度
function queueNextStreamTranscriptPatch() {
  if (streamTranscriptPatchTimer || streamTranscriptPatchRunning) return;
  streamTranscriptPatchTimer = window.setTimeout(() => {
    streamTranscriptPatchTimer = 0;
    void flushScheduledStreamTranscriptPatch();
  }, STREAM_TRANSCRIPT_PATCH_INTERVAL_MS);
}
```

```ts
// 约 1445 行：recordLifecycleTrace — 即使关闭，payload 仍被提前构造
function recordLifecycleTrace(scope, event, payload = {}, traceId = resolveTraceId(scope)) {
  const entry = recordDebugTrace(debugTraceRuntime, { traceId, scope, event, payload: { ... } });
  if (entry) console.debug(`[stream-demo:debug] ${scope}.${event}`, entry);
  return entry;
}
```

### 2.2 `src/寒冬末日/界面同层版/界面/状态栏/components/TranscriptList.vue`

```ts
// 约 395 行：itemsSignature 含 len，token 变一次签名就变
const itemsSignature = computed(() =>
  props.items.map(item => ({
    id: item.message_id,
    phase: item.phase,
    len: item.content.length,
  })),
);
```

```vue
<!-- 约 18 行：v-for 目前没有 v-memo，所有可见卡片每 tick 都会 patch -->
<div v-for="item in visibleItems" :key="item.message_id" class="transcript-entry" ... >
  <component :is="item.isOpening ? TranscriptOpeningCard : TranscriptMessageCard" ... />
</div>
```

### 2.3 `src/寒冬末日/界面同层版/界面/状态栏/components/TranscriptMessageCard.vue`

```ts
// 约 141 行：签名把整个 HTML 字符串 join 进来
const assistantBodySignature = computed(() => {
  if (props.item.role !== 'assistant') return `role:${props.item.role}:${props.item.message_id}`;
  const html = props.item.isStreaming ? props.item.streamHtml : props.item.finalHtml;
  return [
    props.item.message_id,
    props.item.isStreaming ? 'stream' : 'final',
    props.item.phase,
    String(html ?? '').length,
    String(html ?? ''), // ⚠️ 每 tick KB 级字符串
  ].join('::');
});
```

```vue
<!-- 约 89 行：流式分支也走 v-html，浏览器每 tick 重建 DOM 子树 -->
<div v-if="item.isStreaming"
     ref="assistantBodyRef"
     class="assistant-body html-body is-stream-stage"
     :data-message-id="item.message_id"
     v-html="item.streamHtml"></div>
```

```ts
// 约 250 行：每次签名变化都 hydrate + 全量重绑事件
watch(assistantBodySignature, async () => {
  if (props.item.role !== 'assistant') return;
  recordComponentTrace('update');
  await nextTick();
  await hydrateAssistantBodyImages();
  await nextTick();
  bindAssistantBodyInteractions();
}, { immediate: true, flush: 'post' });
```

### 2.4 `src/寒冬末日/界面同层版/界面/状态栏/theme-tokens.css` 等样式

- `theme-tokens.css` 约 249 行：`.demo-surface`  `backdrop-filter: blur(16px)`。
- `StoryPage.vue` 约 1894 / 2000 / 2023 / 2171 / 2215 / 2376 / 2429 / 2539 / 2707 行：共 9 处 blur。
- `TranscriptList.vue` 约 554 行：FAB 的 blur(14px)。
- `TranscriptMessageCard.vue` 约 569 行：图片按钮 blur(10px)。
- `HudModal.vue` 约 74 行：弹窗遮罩 blur(6px)。

### 2.5 `src/寒冬末日/界面同层版/界面/状态栏/global.css`

```css
/* html/body 全局加 0.3s transition —— 流式期间会和 repaint 叠加 */
transition:
  background-color 0.3s ease,
  color 0.3s ease;
```

---

## 3. 优化措施（按优先级分组）

### 🔴 P0 — 必做，收益最大

---

#### P0-1 · 流式阶段不再调用两次 `buildFinalHtml`

**原理**：流式期间正文还在增长，用户看到的就是纯文本；`formatAsDisplayedMessage` + `appendChatu8ArtifactsToHtml` 这两条重管线不会带来可见差异（也不会有图片出现），完全可以延后到 `phase === 'done'` 时跑一次。

**改动文件**：`src/寒冬末日/界面同层版/界面/状态栏/useStreamingDemo.ts`

**改动位置**：`buildTranscriptItem`（约 1262 行）。

**改动前**：
```ts
const streamHtml = isDemoAssistant
  ? buildFinalHtml(renderSource, input.id, strippedRenderSource)
  : buildFinalHtml(displayRenderSource, input.id, strippedRenderSource);
const finalHtml = buildFinalHtml(displayRenderSource, input.id, strippedRenderSource);
```

**改动后**：
```ts
const isStreamingItem = isDemoAssistant && phase === 'stream';

// 流式期间：只产出轻量的 <pre> 文本；finalHtml 置空，stream 结束后再跑真正的渲染
const streamHtml = isStreamingItem
  ? buildStreamStageHtml(streamRenderSource, input.role, input.id)
  : buildFinalHtml(displayRenderSource, input.id, strippedRenderSource);

const finalHtml = isStreamingItem
  ? ''
  : buildFinalHtml(displayRenderSource, input.id, strippedRenderSource);
```

**注意事项**：
- `buildStreamStageHtml` 已经存在（约 231 行），沿用其逻辑即可。
- 一旦 `phase` 切到 `'done'`，原样走 `buildFinalHtml`，保证收尾渲染、图片 artifact 注入与原行为一致。
- 消费方 (`TranscriptMessageCard`) 已经根据 `item.isStreaming` 二选一，不需要改。

**预期效果**：每 tick 省掉 1 次 `formatAsDisplayedMessage`、1 次跨 iframe DOM 扫描、2 次 `createHTMLDocument` 全量 parse、若干正则替换。单 tick JS 耗时下降通常 50% 以上。

---

#### P0-2 · `TranscriptMessageCard` 流式分支走 `v-text`，不再走 `v-html`

**原理**：P0-1 之后 `streamHtml` 本身已经是 `<pre class="stream-stage-pre">{{escaped text}}</pre>`，完全没有真实 HTML 结构，不必让浏览器每 tick parse + 重建子树；用 `v-text` 就地更新文本节点，重排范围仅限 `<pre>`。另外流式阶段不会出现可交互图片，`hydrateAssistantBodyImages` / `bindAssistantBodyInteractions` 都不用跑。

**改动文件**：`src/寒冬末日/界面同层版/界面/状态栏/components/TranscriptMessageCard.vue`

**改动位置**：

1. 模板中流式分支（约 89 行）。
2. `assistantBodySignature` computed（约 141 行）。
3. watch(assistantBodySignature) 回调（约 250 行）。

**改动后（模板）**：
```vue
<!-- 流式阶段：只用 content / streamText，不需要 HTML 结构 -->
<pre
  v-if="item.isStreaming"
  ref="assistantBodyRef"
  class="assistant-body html-body is-stream-stage stream-stage-pre"
  :data-message-id="item.message_id"
>{{ streamDisplayText }}</pre>

<!-- eslint-disable-next-line vue/no-v-html -->
<div
  v-else
  ref="assistantBodyRef"
  class="assistant-body html-body"
  :data-message-id="item.message_id"
  v-html="item.finalHtml || '<p>(空回复)</p>'"
></div>
```

**改动后（script 片段）**：
```ts
const streamDisplayText = computed(() => {
  if (!props.item.isStreaming) return '';
  // item.content 是 extractStreamDemoContent 的结果，正是流式原文
  return String(props.item.content ?? '').trim() || '等待 token…';
});

const assistantBodySignature = computed(() => {
  if (props.item.role !== 'assistant') return `role:${props.item.role}:${props.item.message_id}`;
  if (props.item.isStreaming) {
    // 流式阶段不参与 v-html 重绑，签名只用于强制更新文本长度
    return `stream:${props.item.message_id}:${streamDisplayText.value.length}`;
  }
  const html = props.item.finalHtml;
  return `final:${props.item.message_id}:${props.item.phase}:${String(html ?? '').length}`;
});

watch(
  assistantBodySignature,
  async () => {
    if (props.item.role !== 'assistant') return;
    if (props.item.isStreaming) return; // 流式阶段不需要 hydrate/rebind
    recordComponentTrace('update');
    await nextTick();
    await hydrateAssistantBodyImages();
    await nextTick();
    bindAssistantBodyInteractions();
  },
  { immediate: true, flush: 'post' },
);
```

**注意事项**：
- 确认 `.stream-stage-pre` 样式在 `<pre>` 节点上工作正常（原来它落在 `<div class="assistant-body"> <pre>`，现在把类挂到 `<pre>` 本身；`theme-tokens.css` 和 `TranscriptMessageCard.vue` 原有 `:deep(.stream-stage-pre)` 规则仍然可以命中，但需要冒烟一下确认）。
- 进入 done 时，`isStreaming` 翻 false，watcher 会被触发一次，走正常 hydrate/rebind 路径。
- 如果需要用 `item.streamHtml` 作为 fallback（比如非 demo assistant 且处于 streaming 状态），可以把上面的 `streamDisplayText` 换成：
  ```ts
  return String(props.item.content ?? props.item.streamHtml ?? '').replace(/<[^>]+>/g, '').trim() || '等待 token…';
  ```

**预期效果**：
- 流式阶段每 tick 不再触发 `v-html` 子树重建、不再重绑图片事件。
- 手机端文本更新开销接近 "刷新一个 text node"。

---

#### P0-3 · `TranscriptList.itemsSignature` 去掉 `len` 字段

**原理**：这个签名的真实用途是"新增楼层时锚底 + 历史分页游标计算"。当前把 `content.length` 放进去，等于每 token 都会触发一次滚动锚定逻辑（`resolveTranscriptStartIndexOnItemsChange` + `nextTick` + 可能的 `scrollTop` 赋值），手机上尤其敏感。

**改动文件**：`src/寒冬末日/界面同层版/界面/状态栏/components/TranscriptList.vue`

**改动位置**：约 395 行的 `itemsSignature`。

**改动后**：
```ts
const itemsSignature = computed(() =>
  props.items.map(item => `${item.message_id}:${item.phase}`).join(','),
);
```

**注意事项**：
- `watch(itemsSignature, async (nextSignature, previousSignature) => { ... })` 里之前通过 `previousSignature?.map(...)` 拿 ids。签名改成字符串后要相应调整：
  ```ts
  watch(itemsSignature, async (next, prev) => {
    const previousIds = prev ? prev.split(',').map(entry => Number(entry.split(':')[0])).filter(Number.isFinite) : [];
    const nextIds = next ? next.split(',').map(entry => Number(entry.split(':')[0])).filter(Number.isFinite) : [];
    const appendedNewFloor = didTranscriptAppendNewFloor({ previousIds, nextIds });
    // 余下逻辑保持不变
  });
  ```
- 流式期间 `phase` 不变（一直是 `stream`），签名稳定；phase 切到 `done` 时签名变化一次，触发锚底。
- 新楼层插入时签名也会变化，锚底正常。

**预期效果**：流式期间不再反复进入滚动锚定分支，主线程负担 ↓。

---

#### P0-4 · `syncTranscriptFlags` 在 flag 未变时保持引用相等

**原理**：Vue 的 diff 严重依赖 props 引用相等。只要 `syncTranscriptFlags` 每次都产出新对象，`TranscriptList` 的所有可见卡片都会 patch；配合 P0-2 后卡片内 watcher 不再做重活，但组件级 patch 仍要走 vnode diff。让未变条目保留原引用即可让 diff 短路。

**改动文件**：`src/寒冬末日/界面同层版/界面/状态栏/useStreamingDemo.ts`

**改动位置**：约 2189 行 `syncTranscriptFlags`。

**改动后**：
```ts
function syncTranscriptFlags(items: TranscriptItem[]): TranscriptItem[] {
  const latestId = assistantMessageId.value;
  const isStreamingStatus = status.value === 'streaming';
  return items.map(item => {
    const nextIsLatest = latestId === item.message_id;
    const nextIsStreaming = nextIsLatest && (isStreamingStatus || item.phase === 'stream');
    if (item.isLatest === nextIsLatest && item.isStreaming === nextIsStreaming) {
      return item;
    }
    return { ...item, isLatest: nextIsLatest, isStreaming: nextIsStreaming };
  });
}
```

**注意事项**：
- 需要保证 `buildTranscriptItem` 返回的对象里，`isLatest` / `isStreaming` 字段值与此函数最终目标一致（目前已经一致）。
- 测试需确认 upsert 流程仍然能感知到 streaming → done 的状态切换（`phase` 变化会走 `buildTranscriptItem`，对象重建，引用自然变）。

**预期效果**：流式期间，历史楼层条目引用保持稳定，`TranscriptList` 的 v-for diff 基本只 patch 正在流式的那一条。

---

### 🟠 P1 — 强烈建议，改动面小、收益明显

---

#### P1-5 · 移动端关闭 `backdrop-filter`

**原理**：移动端 GPU 对 blur 非常敏感；当流式期间下方 transcript 容器不断 invalidate 时，顶部/侧边/底部这些 blur 层每帧都要合成一次。实测 9 处 blur 叠加是手机端 jank 的主要视觉成因之一。

**改动文件**：优先放在 `theme-tokens.css`；如果有冲突，也可以在 `global.css` 末尾追加。

**改动内容**（追加即可）：
```css
@media (max-width: 760px) {
  .hud-panel,
  .demo-surface,
  .ui-top-toolbar,
  .ui-side-drawer,
  .ui-bottom-drawer,
  .ui-drawer-pill,
  .transcript-fab,
  .image-tag-button,
  .hud-modal-backdrop,
  .assistant-body-wrap :deep(.image-tag-button) {
    backdrop-filter: none !important;
    -webkit-backdrop-filter: none !important;
  }

  /* 去掉 blur 后补回背景，避免后面内容穿透 */
  .ui-top-toolbar,
  .ui-side-drawer,
  .ui-bottom-drawer {
    background-color: color-mix(in srgb, var(--background) 94%, var(--surface) 6%);
  }
}

/* 老用户：关闭 prefers-reduced-motion 情况下也降级 */
@media (prefers-reduced-motion: reduce) {
  * {
    backdrop-filter: none !important;
    -webkit-backdrop-filter: none !important;
  }
}
```

**注意事项**：
- 具体选择器以实际类名为准，Vue `scoped` 样式需要在同一个 vue 文件中改、或改用 `:deep()` 选择子组件类。
- 执行前先把 `grep backdrop-filter` 的 9 处命中梳理一份清单，逐个确认有无剩漏。
- 视觉设计同学如果对"毛玻璃"有强要求，可以保留桌面端，仅在手机端降级。

**改动文件补充**：
- `src/寒冬末日/界面同层版/界面/状态栏/components/TranscriptMessageCard.vue` ≥ `@media (max-width: 760px)` 中添加 `.assistant-corners { display: none; }`（目前只对 density-minimal 隐藏，手机端即便普通密度也建议关掉，节省 4 个绝对定位子节点）。

**预期效果**：手机端滚动帧率提升明显；合成层数量大幅下降，可在 DevTools Performance 的 "Layers" 面板直接看到差异。

---

#### P1-6 · 流式期间跳过 `appendChatu8ArtifactsToHtml`

**原理**：这个函数负责把图片锚点注入到 HTML 中。流式期间图片还没生成、锚点也没写回消息层，等于空跑；而它内部要跨 iframe 查 5 个 selector、创建一次 HTMLDocument parse HTML、再扫描 img。

**改动文件**：`src/寒冬末日/界面同层版/界面/状态栏/useStreamingDemo.ts`

**改动位置**：`buildFinalHtml`（约 256 行）或 `applyTranscriptArtifacts` 的调用方。

**改动方案 A（推荐，最小侵入）**：
在 `buildFinalHtml` 内加一个轻量 fast path，由调用方传入一个 phase 提示：
```ts
function buildFinalHtml(
  renderSource: string,
  message_id: number,
  artifactSource?: string,
  options: { skipArtifacts?: boolean } = {},
): string {
  // ...原逻辑产出 html...
  if (options.skipArtifacts) return html;
  return applyTranscriptArtifacts({
    html,
    renderSource: artifactSource ?? renderSource,
    messageId: message_id,
    appendArtifacts: appendChatu8ArtifactsToHtml,
  });
}
```

再在 `buildTranscriptItem` 里 streaming 分支传 `{ skipArtifacts: true }`。如果已经做了 P0-1，这条基本已经自动生效，只是把 fast path 加在函数里，以防其他路径（比如未来新调用方）忘记判断。

**改动方案 B（更彻底）**：
给 `appendChatu8ArtifactsToHtml` 的开头加一个守卫：
```ts
function appendChatu8ArtifactsToHtml(html, renderSource, messageId) {
  // 流式期间正文极短且没有 prompt token，早退
  const promptTokens = collectChatu8PromptTokens(renderSource);
  if (promptTokens.length === 0 && !hasRenderedImagesInSource(renderSource)) {
    return html;
  }
  // ...原逻辑
}
```

二选一即可。

**预期效果**：P0-1 已经覆盖大部分场景；本条作为兜底，把 `regenerate`、`edit` 等非流式路径里的重复工作也一并优化。

---

#### P1-7 · 调试日志惰性化

**原理**：`recordLifecycleTrace` / `debugConsoleLog` 在 `debugTraceRuntime.enabled === false` 时也会先把 payload 构造出来。流式热路径上的 `summarizeTranscriptForDebug(transcript.value)` 不仅是 map，还会 slice 预览文本。

**改动文件**：`src/寒冬末日/界面同层版/界面/状态栏/useStreamingDemo.ts`

**改动方案**（两种都做）：

1. 重构 `recordLifecycleTrace` 支持 lazy payload：
   ```ts
   function recordLifecycleTrace(
     scope: string,
     event: string,
     payload: Record<string, unknown> | (() => Record<string, unknown>) = {},
     traceId = resolveTraceId(scope),
   ) {
     if (!debugTraceRuntime.enabled) return null;
     const resolvedPayload = typeof payload === 'function' ? payload() : payload;
     const entry = recordDebugTrace(debugTraceRuntime, {
       traceId, scope, event,
       payload: { busy: busy.value, status: status.value, assistantMessageId: assistantMessageId.value, ...resolvedPayload },
     });
     if (entry) console.debug(`[stream-demo:debug] ${scope}.${event}`, entry);
     return entry;
   }
   ```

2. 把 **流式热路径**上的调用（`patchAssistantMessage`、`rebuildTranscript`、`handleHostRefreshEvent`、`scheduleUiRefresh`、`bindGenerationEvents` 的 token 分支）都改成 lazy 形式：
   ```ts
   recordLifecycleTrace('patchAssistantMessage', 'commit_done', () => ({
     phase, sequence, messageId, nextSignature,
     transcriptAssistantSummary: summarizeTranscriptForDebug(transcript.value),
   }), traceId);
   ```

**注意事项**：
- 冷路径（opening 初始化、回退删除、生成结束等）不急着改，避免扩散修改面。
- `recordComponentDebugTrace` 在 `TranscriptMessageCard` 里每次 watch 都跑，可以直接加一层：
  ```ts
  function recordComponentTrace(event, payload = {}) {
    const runtime = (window as any).__STREAM_DEMO_DEBUG__;
    if (!runtime?.enabled) return;
    recordComponentDebugTrace({ scope: 'TranscriptMessageCard', event, payload: { ... } });
  }
  ```

**预期效果**：关闭调试时，流式 tick 中的隐性分配基本清零。

---

#### P1-8 · 流式节流自适应 + `requestAnimationFrame` 对齐

**原理**：80 ms ≈ 12.5 Hz 刷新，在低端手机（60 Hz 屏幕 + 弱 CPU）即便不卡顿也常错帧。适当放宽到 120~180 ms（视觉感知不明显）；更好的做法是用 `requestAnimationFrame` 聚合，让 UI 更新和屏幕刷新对齐。

**改动文件**：`src/寒冬末日/界面同层版/界面/状态栏/useStreamingDemo.ts`

**改动方案 A（最小改动）**：
```ts
const STREAM_TRANSCRIPT_PATCH_INTERVAL_MS = (() => {
  try {
    const cores = Number(navigator.hardwareConcurrency ?? 8);
    const isMobileLike = /Mobi|Android|iPhone|iPad/i.test(navigator.userAgent);
    if (isMobileLike || cores <= 4) return 160;
    return 80;
  } catch {
    return 80;
  }
})();
```

**改动方案 B（推荐，但要更仔细测试）**：
把 setTimeout 换成 rAF 聚合：
```ts
function queueNextStreamTranscriptPatch() {
  if (streamTranscriptPatchTimer || streamTranscriptPatchRunning) return;
  streamTranscriptPatchTimer = window.requestAnimationFrame(() => {
    streamTranscriptPatchTimer = 0;
    void flushScheduledStreamTranscriptPatch();
  });
}

function cancelScheduledStreamTranscriptPatch() {
  if (streamTranscriptPatchTimer) {
    window.cancelAnimationFrame(streamTranscriptPatchTimer);
    streamTranscriptPatchTimer = 0;
  }
  streamTranscriptPatchDirty = false;
}
```
> 注意 `streamTranscriptPatchTimer` 的类型变化（`number`）和取消方式。

**预期效果**：
- 每 patch 的合并窗口自适应到设备能力。
- rAF 方案能避免跨帧多次 repaint，滚动/打字输入更流畅。

---

### 🟡 P2 — 锦上添花，可视情况做

---

#### P2-9 · `v-for` 上加 `v-memo`

**改动文件**：`src/寒冬末日/界面同层版/界面/状态栏/components/TranscriptList.vue`

```vue
<div
  v-for="item in visibleItems"
  :key="item.message_id"
  v-memo="[item.message_id, item.phase, item.isStreaming, item.isLatest, item.finalHtml?.length ?? 0]"
  class="transcript-entry"
  :data-message-id="item.message_id"
  style="position: relative"
>
  ...
</div>
```

配合 P0-4 后，历史条目 memo key 数组保持引用相等 → 彻底跳过 vnode diff。

---

#### P2-10 · 减少 `global.css` 的全局 transition

**改动文件**：`src/寒冬末日/界面同层版/界面/状态栏/global.css`

```css
/* 改为只在主题切换时触发 transition，避免长期开启 */
html, body {
  /* transition: background-color 0.3s ease, color 0.3s ease;  — 去掉 */
}

html[data-theme-transition="1"] {
  transition:
    background-color 0.3s ease,
    color 0.3s ease;
}
```
并在切换主题的 JS 入口里临时加上 `data-theme-transition="1"`，500ms 后移除。

---

#### P2-11 · MutationObserver 节流

`generatedImageDomObserver` 回调内部已经有 `generatedImageDomMutationTimer` 变量但未使用；建议统一加一个 50~80 ms 的防抖：
```ts
generatedImageDomObserver = new MutationObserver(records => {
  if (!records.some(hasRelevantChatu8Mutation)) return;
  if (generatedImageDomMutationTimer) return;
  generatedImageDomMutationTimer = window.setTimeout(() => {
    generatedImageDomMutationTimer = 0;
    syncPendingRequestHintsFromDom();
    // ...
  }, 80);
});
```

---

#### P2-12 · `TranscriptMessageCard` 四角隐藏范围扩大

把 `@media (max-width: 760px)` 中无条件隐藏 `.assistant-corners`（目前只在 `density-minimal` 下隐藏）。

---

## 4. 一键 checklist（给施工 AI）

### 必做

- [ ] `useStreamingDemo.ts:buildTranscriptItem` → 流式阶段只调 `buildStreamStageHtml`，`finalHtml` 置空。
- [ ] `TranscriptMessageCard.vue` → 流式分支改为 `<pre v-text>`；`assistantBodySignature` 移除 HTML 内容；watcher 在 streaming 时早退。
- [ ] `TranscriptList.vue:itemsSignature` → 移除 `len`，并修改 watch 里的 ids 取值逻辑。
- [ ] `useStreamingDemo.ts:syncTranscriptFlags` → 未变条目返回原引用。

### 强烈建议

- [ ] 在 `theme-tokens.css` 追加 `@media (max-width: 760px)` 关闭 `backdrop-filter`，并追加 `prefers-reduced-motion` 降级块。
- [ ] `buildFinalHtml` / `appendChatu8ArtifactsToHtml` 加 streaming 早退守卫。
- [ ] `recordLifecycleTrace` 支持 lazy payload，热路径改成 lazy。
- [ ] `STREAM_TRANSCRIPT_PATCH_INTERVAL_MS` 根据设备判断；或把调度改成 `requestAnimationFrame`（同时更新 cancel 逻辑）。

### 可选

- [ ] `TranscriptList.vue` v-for 加 `v-memo`。
- [ ] `global.css` 主题切换条件化 transition。
- [ ] `generatedImageDomObserver` 回调加 50~80 ms 防抖。
- [ ] `TranscriptMessageCard.vue` `@media (max-width: 760px)` 隐藏 `.assistant-corners`。

---

## 5. 测试建议

### 5.1 现有自动化测试

- `src/寒冬末日/界面同层版/界面/状态栏/__tests__/streamingFlow.test.js`：流式链路快照测试。改动 P0-1 / P0-2 后必须通过；如果断言检查了 `finalHtml` 内容，需要在流式阶段接受空串或改写断言。
- `__tests__/transcriptRenderIsolation.test.js`：transcript / gallery 解耦测试。P0-4 改动不应破坏这里的语义。
- `__tests__/refreshBoundarySource.test.js`：refresh 边界测试。P1-6 若增加 options 参数，请确认无副作用。
- `__tests__/assistantMessageRefreshMode.test.js`：不受影响，保持通过。
- `__tests__/transcriptImagePersistence.test.ts` / `generatedImageInteraction.test.ts`：图片 hydrate/rebind 路径。P0-2 在 streaming 下跳过 hydrate，这些测试主要覆盖非流式场景，需要确认 streaming case（若有）被正确标记。
- 运行：`pnpm -C . test`（或仓库现有 `pnpm` 脚本，参考 `package.json`；如果只是跑这批可直接 `pnpm vitest --run src/寒冬末日/界面同层版` 或 `node --test` 对应的 `.test.js`）。

### 5.2 手动冒烟

1. **流式输入**（必测）：
   - 用 MCP `127.0.0.1:8000`、聊天 `末世寒冬-星穹秩序`，发起一次生成。
   - 手机端用 Chrome 远程调试接入，Performance 录制 5 秒流式过程，确认：
     - Main 栏不再出现连续红色 "Long task"（>50ms）。
     - `patchAssistantMessage` / `buildTranscriptItem` 的累计时间显著下降。
     - Layer 面板合成层数下降。
2. **流式完成后**：
   - 确认最终 `finalHtml` 正常渲染，图片 artifact、button、image-view / regenerate 交互全都正常。
   - 在 streaming 中途点击 "改词重生"、"回退删除"、"查看详情" 仍然有效。
3. **滚动锚定**：
   - 流式期间手动上滑 → 应进入"浏览历史"。
   - 流式结束后新楼层出现 → 应回到"跟随最新"并锚底。
   - 确认 `itemsSignature` 改造没有破坏该逻辑。
4. **图片链路**（关键）：
   - 流式结束后正文出现 prompt token → 触发生图 → 图片应出现在正文锚点附近和图廊中。
   - 双击 / 长按已有图片，仍可走 view / regenerate。
5. **CSS 降级**：
   - 手机端（或 DevTools "Responsive" 模式，宽 ≤ 760）确认各面板仍有可读背景，不应透出底下内容。
   - 桌面端应保持原有毛玻璃效果。
6. **调试开关**：
   - 在 DevTools console 设置 `localStorage.setItem('stream_demo_debug', '1')` 后刷新，确认日志仍然齐全。
   - 关闭后确认 console 无输出、主线程无多余分配（用 Memory 采样对比前后 `ObjectShape` 增量）。

### 5.3 性能验证指标

- **定量**（手机端 Chrome Performance）：
  - 单次流式 patch 主线程耗时：从 P50 ~25ms、P95 ~60ms 下降到 P50 <10ms、P95 <25ms。
  - 合成层数量：目标 <20（原先约 30+）。
  - FPS：流式滚动过程中保持 >45 fps（原先常跌到 20 左右）。
- **定性**：
  - 手指滑动、输入框打字、切换抽屉的交互不再"顿一下"。
  - 文本更新可见卡顿消失。

---

## 6. 回滚策略

- 所有改动都可独立回滚：
  - P0-1 / P0-2 / P0-4 集中在三处函数 / 两个模板，Git diff 可视；单独 revert 也不会破坏业务。
  - P0-3 / P1-6 / P1-7 / P1-8 都是守卫或参数化，改坏之后可以快速加回。
  - P1-5 CSS 回滚只需删除 `@media` 块。
- 建议按 P0 → P1 → P2 顺序分三次提交，每次提交后跑一次 `pnpm test` + 手机端冒烟。

---

## 7. 风险提示

1. **流式 `finalHtml` 置空**：如果下游有地方在 streaming 期间读取 `finalHtml`（搜索 `item.finalHtml` 的所有用处），改造后可能拿到空串。目前确认：
   - `TranscriptMessageCard`：通过 `isStreaming` 走不同分支。
   - `TranscriptList` 等未直接读。
   - 若后续新增消费方，需给它加 `isStreaming` 判断或 fallback。
2. **`syncTranscriptFlags` 保留引用**：如果其它地方依赖"每次 rebuild 都产出新对象"来触发 Vue 响应式，需要确认没有 `shallowRef` 依赖（代码里没发现）。
3. **`backdrop-filter` 降级**：设计观感会变化，提前同步设计同学；桌面端不受影响。
4. **`requestAnimationFrame` 聚合**：如果宿主 iframe 因被 throttle（后台标签页）导致 rAF 停止，`streamText` 会短暂不更新。GENERATION_ENDED 时的 `flushScheduledStreamTranscriptPatch` 兜底能救，但建议测试后台切换场景。
5. **lazy payload 重构**：修改 `recordLifecycleTrace` 签名是广播式改动，请在同一次 commit 中一起改所有热点调用，避免半成品状态。

---

## 8. 附录：优化前后主流程对比

### 8.1 优化前单次 patch

```
STREAM_TOKEN
  ↓ 80ms 节流
patchAssistantMessage('stream')
  ↓
setChatMessages({refresh:'none'})
upsertTranscriptItem
  ↓
buildTranscriptItem
  ├─ applyRegexForDisplay   (跑所有 display 层正则)
  ├─ buildFinalHtml(streamHtml)   <-- ①
  │    ├─ formatAsDisplayedMessage
  │    ├─ normalizeDisplayedHtml  (2× replace)
  │    └─ applyTranscriptArtifacts
  │         └─ appendChatu8ArtifactsToHtml
  │              ├─ resolveIframeAssistantRoots (跨 iframe × 5 selector)
  │              ├─ readNativeFirstRenderableImagesForMessage
  │              └─ collectImageSourceIdentitiesFromHtml  (createHTMLDocument parse)
  └─ buildFinalHtml(finalHtml)   <-- ②  (① 整套再来一遍)
  ↓
syncTranscriptFlags(clipTranscriptItemsForUi([...]))  (全部浅拷贝)
  ↓
transcript.value = newArray
  ↓
visibleTranscript recompute
  ↓
TranscriptList itemsSignature 变 (len 变了)
  ↓
watch 回调 → resolveStartIndex + nextTick + 可能 scrollTop 赋值
  ↓
所有可见 TranscriptMessageCard patch
  ↓
assistantBodySignature 变 (html 内容)
  ↓
watch 回调 → nextTick → hydrateAssistantBodyImages
  ↓ nextTick
bindAssistantBodyInteractions (全量拆/绑事件)
  ↓
v-html 子树重建 + DOM 重排 + GPU 合成 (9 处 blur)
```

### 8.2 优化后单次 patch

```
STREAM_TOKEN
  ↓ 自适应节流（或 rAF）
patchAssistantMessage('stream')
  ↓
setChatMessages({refresh:'none'})
upsertTranscriptItem
  ↓
buildTranscriptItem  (isStreamingItem=true)
  └─ buildStreamStageHtml  (一次轻量 escape)
  ↓
syncTranscriptFlags  (未变条目保留引用)
  ↓
transcript.value = newArray
  ↓
TranscriptList itemsSignature 不变
  → v-memo 命中，除 streaming 条目外 0 patch
  ↓
仅 streaming 卡 patch
  ↓
assistantBodySignature 只含 length，变化
  ↓
watch 回调直接 return（isStreaming 早退）
  ↓
<pre v-text> 文本节点就地更新
  ↓
GPU 合成（手机端已关 blur）
```

---

## 9. 参考文件速查

- `src/寒冬末日/界面同层版/界面/状态栏/useStreamingDemo.ts`
- `src/寒冬末日/界面同层版/界面/状态栏/components/TranscriptMessageCard.vue`
- `src/寒冬末日/界面同层版/界面/状态栏/components/TranscriptList.vue`
- `src/寒冬末日/界面同层版/界面/状态栏/pages/StoryPage.vue`
- `src/寒冬末日/界面同层版/界面/状态栏/theme-tokens.css`
- `src/寒冬末日/界面同层版/界面/状态栏/global.css`
- `src/寒冬末日/界面同层版/界面/状态栏/debugTrace.ts`
- `src/寒冬末日/界面同层版/界面/状态栏/transcriptDomRefresh.ts`
- `src/寒冬末日/界面同层版/界面/状态栏/refreshDomains.ts`
- `src/寒冬末日/界面同层版/界面/状态栏/assistantMessageRefreshMode.ts`
- `src/寒冬末日/界面同层版/开发文档.md`
- `src/寒冬末日/界面同层版/界面/状态栏/__tests__/`（所有 `streamingFlow*`、`transcriptRenderIsolation*`、`assistantMessageRefreshMode*`、`transcriptImagePersistence*` 测试）

---

*最后更新：2026-05-09*
